# @format

version: '3.8'
services:
  db:
    image: mysql:8.0
    container_name: esun-mysql
    restart: always
    environment:
      MYSQL_DATABASE: esun_db
      MYSQL_ROOT_PASSWORD: password123
    ports:
      - '3306:3306'
    volumes:
      - db_data:/var/lib/mysql

volumes:
  db_data:
